#### 🔐 项目一：智能门锁 (STM32/GD32 + 指纹 + 低功耗)

这个项目是典型的“控制逻辑密集型”项目，重点在于裸机状态机和低功耗处理。

- [ ] **指纹模块通信协议解析.md** 
  - [ ] 重点分析：UART数据帧的包头、指令码、校验和计算。
  - [ ] 难点复盘：如何处理串口接收的不定长数据？是否使用了空闲中断或超时判断？
- [ ] **低功耗模式与唤醒策略详解.md** 
  - [ ] 重点分析：STM32/GD32 的 Stop/Standby 模式配置。
  - [ ] 难点复盘：由于是指纹锁，如何实现“触摸即唤醒”？中断优先级的配置是怎样的？

#### 🔔 项目二：智能门铃 (ESP32 + 音视频 + 网络)

这个项目涉及复杂的音视频流和网络通信，是展示“全栈”能力的窗口。

- [ ] **ESP32音频驱动(I2S)与编解码流程.md** 
  - [ ] 重点分析：I2S 总线协议（BCLK, WS, SD），ES8311/AC101 等 Codec 芯片的寄存器配置。
  - [ ] 难点复盘：出现杂音或爆破音时是如何排查时钟配置的？
- [ ] **HTTP/WebSocket 图像传输机制.md** 
  - [ ] 重点分析：摄像头采集的数据（JPEG流）是如何打包通过网络发给上位机的？
  - [ ] 难点复盘：TCP 粘包处理或 UDP 丢包的容错机制。

#### 🔋 项目三：BMS 电池管理系统 (ADC + 算法 + 保护)

BMS 是目前新能源行业非常火的方向，面试时重点考察模拟量采集精度。

- [ ] **高精度 ADC 采集与滤波算法.md** 
  - [ ] 重点分析：多通道 ADC + DMA 的配置流程。
  - [ ] 难点复盘：使用了哪种软件滤波算法（中位值、滑动平均、卡尔曼）？如何校准电压偏差？
- [ ] **电池电量(SOC)估算逻辑分析.md **
  - [ ] 重点分析：虽然可能用的是安时积分法或查表法，需要把逻辑图画出来。
  - [ ] 难点复盘：如何检测过压、欠压、过流并触发保护中断？

#### 🐮 项目四：畜牧定位器 (GPS + LoRa + 低功耗)

典型的“广域物联网”项目，突出长距离通信和定位解析。

- [ ] **NMEA-0183 协议解析与 GPS 漂移处理.md **
  - [ ] 重点分析：`$GNRMC` 或 `$GNGGA` 语句的字符串解析函数编写。
  - [ ] 难点复盘：如何提取经纬度并转化为地图可用的坐标？
- [ ] **LoRa 扩频通信与 CAD 侦听模式.md **
  - [ ] 重点分析：LoRa 的工作原理（扩频因子 SF、带宽 BW 对距离的影响）。
  - [ ] 难点复盘：如何实现点对点（P2P）的长距离低功耗发送？

#### ⚙️ 项目五：电机控制与网关 (CAN + Modbus + 步进电机)

这是工业控制领域的“黄金组合”，是含金量最硬的部分。

- [ ] **CAN 总线过滤器(Filter)与波特率计算.md **
  - [ ] 重点分析：标识符屏蔽位模式（Mask Mode）和列表模式（List Mode）的区别与配置。
  - [ ] 难点复盘：仲裁机制是如何工作的？如果有多个 ID 同时发送，总线如何处理？
- [ ] **Modbus RTU 从机协议栈实现.md **
  - [ ] 重点分析：手写一段 Modbus 解析代码，包括 CRC16 校验函数的实现。
  - [ ] 难点复盘：如何处理 485 通信的收发切换延时问题？
- [ ] **步进电机 S 型/梯形加减速算法.md **
  - [ ] 重点分析：从简单的 PWM 调频到带有加减速曲线的脉冲控制。
  - [ ] 难点复盘：如何避免电机启动时的丢步或堵转？

### 💧 项目六：智能加湿器 (ESP32-C3 + IoT)

> **核心价值**：展示你对 ESP32 特有外设（LEDC/RMT）的掌控，以及对物联网应用层协议（MQTT/JSON）的实战应用。

#### 1. 外设驱动深度解析

- [ ] **ESP32 LEDC (PWM) 驱动原理与雾化控制.md**
  - [ ] **核心分析**：ESP32 的 LEDC 外设与普通 STM32 定时器 PWM 的区别（它专门用于 LED 和电机，支持硬件渐变）。
  - [ ] **关键代码**：分析 `ledc_channel_config` 和 `ledc_timer_config` 结构体配置。
  - [ ] **难点复盘**：雾化片通常需要特定频率（如 108kHz）才能工作，如何精准配置分频系数？
- [ ] **单总线时序分析：DHT11/DHT20 温湿度驱动.md**
  - [ ] **核心分析**：画出微秒级（us）的时序图（起始信号、响应信号、数据 0/1 的电平宽度）。
  - [ ] **面试考点**：如果读取数据经常校验失败（Checksum Error），通常是什么原因？（延时函数精度、中断干扰保护）。

#### 2. 物联网协议与云端交互

- [ ] **MQTT 协议接入阿里云全流程详解.md** 
  - [ ] **核心分析**：图解设备如何通过 `ProductKey`、`DeviceName`、`DeviceSecret` 进行三元组鉴权。
  - [ ] **关键代码**：分析 MQTT 的 `CONNECT`, `SUBSCRIBE`, `PUBLISH` 报文构建。
  - [ ] **面试考点**：QoS 0, 1, 2 的区别？在加湿器场景中，上报温湿度用 QoS 几？下发控制指令用 QoS 几？
- [ ] **cJSON 数据封装与解析实战.md **
  - [ ] **核心分析**：如何使用 cJSON 库将 C 语言结构体（温湿度数据）“序列化”为 JSON 字符串发送给云端。
  - [ ] **关键代码**：反之，如何解析云端下发的 JSON 控制指令（如 `{"power": "on", "target_humidity": 60}`）。
  - [ ] **难点复盘**：解析 JSON 时的内存泄漏问题（也就是用完记得 `cJSON_Delete`）。

#### 3. 系统功能逻辑

- [ ] **NVS (Non-Volatile Storage) 掉电保存参数.md **
  - [ ] **核心分析**：ESP32 的 NVS 分区读写机制（Key-Value 结构）。
  - [ ] **应用场景**：WiFi 账号密码、用户设定的目标湿度，掉电后如何恢复？
- [ ] **SNTP 网络校时与 RTC 同步.md **
  - [ ] **核心分析**：设备联网后如何通过 NTP 服务器获取 UTC 时间，并转换为本地时间显示在 OLED 上。



#### 🧠 通用技术栈 (FreeRTOS + 协议)

无论做哪个项目，这些都是底层的通用能力。

- [ ] **FreeRTOS 任务调度与堆栈溢出分析.md **
  - [ ] 核心点：抢占式调度原理，任务优先级的分配原则。
  - [ ] 面试题：如何调试 HardFault？如何确定任务堆栈该给多大？
- [ ] **FreeRTOS 队列(Queue)与信号量(Semaphore)对比.md **
  - [ ] 核心点：在你的项目中，哪些场景用了二值信号量做同步？哪些用了队列传数据？